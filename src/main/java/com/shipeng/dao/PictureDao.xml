<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shipeng.dao.PictureDao">
	<select id="list" resultType="Picture">
		select * from picture
	</select>
	<insert id="add">
		insert into picture values(null,#{magger})
	</insert>
</mapper>